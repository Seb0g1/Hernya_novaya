# Nginx: иначе POST /api/media отдаёт 413 Request Entity Too Large (лимит по умолчанию ~1m).
# Добавьте в блок server { } для вашего сайта (например tiktok.sebog1.ru), затем:
#   sudo nginx -t && sudo systemctl reload nginx

client_max_body_size 150m;
